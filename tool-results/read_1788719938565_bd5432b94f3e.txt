     1→     1→"use client";
     2→     2→
     3→     3→import * as React from "react";
     4→     4→import { AnimatePresence, motion } from "framer-motion";
     5→     5→import {
     6→     6→  ChevronDown,
     7→     7→  ChevronLeft,
     8→     8→  ChevronRight,
     9→     9→  ChevronUp,
    10→    10→  Clock,
    11→    11→  Download,
    12→    12→  ExternalLink,
    13→    13→  FileImage,
    14→    14→  FileText,
    15→    15→  Folder,
    16→    16→  FolderOpen,
    17→    17→  Home,
    18→    18→  Maximize,
    19→    19→  Menu,
    20→    20→  Minimize,
    21→    21→  MoreVertical,
    22→    22→  PanelLeftClose,
    23→    23→  PanelLeftOpen,
    24→    24→  Search,
    25→    25→  Share2,
    26→    26→  X,
    27→    27→  ZoomIn,
    28→    28→  ZoomOut,
    29→    29→} from "lucide-react";
    30→    30→
    31→    31→import { Button } from "@/components/ui/button";
    32→    32→import { Badge } from "@/components/ui/badge";
    33→    33→import { Skeleton } from "@/components/ui/skeleton";
    34→    34→import { ScrollArea } from "@/components/ui/scroll-area";
    35→    35→import { useAppStore } from "@/lib/store";
    36→    36→import { fromNow, formatDateTime } from "@/lib/format";
    37→    37→import type { Note, Category } from "@/lib/types";
    38→    38→import { cn } from "@/lib/utils";
    39→    39→
    40→    40→type SidebarMode = "list" | "tile";
    41→    41→
    42→    42→// ─── Tree node type ───────────────────────────────────────────
    43→    43→interface TreeNode {
    44→    44→  category: Category;
    45→    45→  topics: { category: Category; notes: Note[] }[];
    46→    46→  notes: Note[];
    47→    47→}
    48→    48→
    49→    49→function buildTree(categories: Category[], notes: Note[]): TreeNode[] {
    50→    50→  const top = categories.filter((c) => !c.parentId);
    51→    51→  return top.map((cat) => {
    52→    52→    const topics = categories
    53→    53→      .filter((c) => c.parentId === cat.id)
    54→    54→      .map((sub) => ({
    55→    55→        category: sub,
    56→    56→        notes: notes
    57→    57→          .filter((n) => n.categoryId === sub.id)
    58→    58→          .sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()),
    59→    59→      }))
    60→    60→      .filter((t) => t.notes.length > 0);
    61→    61→    const directNotes = notes
    62→    62→      .filter((n) => n.categoryId === cat.id)
    63→    63→      .sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime());
    64→    64→    return { category: cat, topics, notes: directNotes };
    65→    65→  }).filter((n) => n.topics.length > 0 || n.notes.length > 0);
    66→    66→}
    67→    67→
    68→    68→// ─── Main NoteViewer ──────────────────────────────────────────
    69→    69→export function NoteViewer() {
    70→    70→  const note = useAppStore((s) => s.selectedNote);
    71→    71→  const loading = useAppStore((s) => s.noteLoading);
    72→    72→  const allNotes = useAppStore((s) => s.notes);
    73→    73→  const allCategories = useAppStore((s) => s.categories);
    74→    74→  const openNote = useAppStore((s) => s.openNote);
    75→    75→  const goHome = useAppStore((s) => s.goHome);
    76→    76→
    77→    77→  // Sidebar state
    78→    78→  const [sidebarCollapsed, setSidebarCollapsed] = React.useState(false);
    79→    79→  const [mobileSidebarOpen, setMobileSidebarOpen] = React.useState(false);
    80→    80→  const [sidebarMode, setSidebarMode] = React.useState<SidebarMode>("list");
    81→    81→  const [expandedCats, setExpandedCats] = React.useState<Set<string>>(new Set());
    82→    82→  const [expandedTopics, setExpandedTopics] = React.useState<Set<string>>(new Set());
    83→    83→  const [sidebarSearch, setSidebarSearch] = React.useState("");
    84→    84→
    85→    85→  // Toolbar / right panel
    86→    86→  const [toolbarExpanded, setToolbarExpanded] = React.useState(true);
    87→    87→  const [rightPanelOpen, setRightPanelOpen] = React.useState(false);
    88→    88→  const [downloadOpen, setDownloadOpen] = React.useState(false);
    89→    89→  const [zoomPresetsOpen, setZoomPresetsOpen] = React.useState(false);
    90→    90→  const [zoom, setZoom] = React.useState(1);
    91→    91→  const [zoomMode, setZoomMode] = React.useState<"fit" | "manual">("fit");
    92→    92→  const [isFullscreen, setIsFullscreen] = React.useState(false);
    93→    93→  const [copied, setCopied] = React.useState(false);
    94→    94→  const [pageInputValue, setPageInputValue] = React.useState("");
    95→    95→  const [pageInputFocused, setPageInputFocused] = React.useState(false);
    96→    96→
    97→    97→  const containerRef = React.useRef<HTMLDivElement>(null);
    98→    98→  const downloadRef = React.useRef<HTMLDivElement>(null);
    99→    99→  const zoomRef = React.useRef<HTMLDivElement>(null);
   100→   100→
   101→   101→  // ─── Build tree ─────────────────────────────────────────────
   102→   102→  const tree = React.useMemo(() => buildTree(allCategories, allNotes), [allCategories, allNotes]);
   103→   103→
   104→   104→  // ─── Pages in current category ─────────────────────────────
   105→   105→  const pages = React.useMemo(() => {
   106→   106→    if (!note) return [];
   107→   107→    const noteCat = allCategories.find((c) => c.id === note.categoryId);
   108→   108→    if (!noteCat) return [note];
   109→   109→    if (noteCat.parentId) {
   110→   110→      return allNotes
   111→   111→        .filter((n) => n.categoryId === noteCat.id)
   112→   112→        .sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime());
   113→   113→    }
   114→   114→    const subIds = allCategories.filter((c) => c.parentId === noteCat.id).map((c) => c.id);
   115→   115→    return allNotes
   116→   116→      .filter((n) => n.categoryId === noteCat.id || subIds.includes(n.categoryId))
   117→   117→      .sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime());
   118→   118→  }, [allNotes, allCategories, note]);
   119→   119→
   120→   120→  const currentIndex = React.useMemo(() => note ? pages.findIndex((p) => p.id === note.id) : -1, [pages, note]);
   121→   121→  const totalPages = pages.length;
   122→   122→  const prevPage = currentIndex > 0 ? pages[currentIndex - 1] : null;
   123→   123→  const nextPage = currentIndex < totalPages - 1 ? pages[currentIndex + 1] : null;
   124→   124→
   125→   125→  const relatedNotes = React.useMemo(() => {
   126→   126→    if (!note) return [];
   127→   127→    return allNotes
   128→   128→      .filter((n) => n.id !== note.id && (n.categoryId === note.categoryId || n.tags?.some((t) => note.tags?.some((nt) => nt.id === t.id))))
   129→   129→      .slice(0, 5);
   130→   130→  }, [allNotes, note]);
   131→   131→
   132→   132→  // ─── Auto-expand active category ───────────────────────────
   133→   133→  React.useEffect(() => {
   134→   134→    if (!note) return;
   135→   135→    const noteCat = allCategories.find((c) => c.id === note.categoryId);
   136→   136→    if (!noteCat) return;
   137→   137→    const parentId = noteCat.parentId ?? noteCat.id;
   138→   138→    setExpandedCats((prev) => new Set([...prev, parentId]));
   139→   139→    if (noteCat.parentId) setExpandedTopics((prev) => new Set([...prev, noteCat.id]));
   140→   140→  }, [note, allCategories]);
   141→   141→
   142→   142→  // ─── Handlers ──────────────────────────────────────────────
   143→   143→  const handleNavigate = React.useCallback((n: Note) => {
   144→   144→    openNote(n);
   145→   145→    if (typeof window !== "undefined" && window.innerWidth < 768) setMobileSidebarOpen(false);
   146→   146→  }, [openNote]);
   147→   147→
   148→   148→  const toggleCat = (id: string) => setExpandedCats((prev) => { const n = new Set(prev); if (n.has(id)) n.delete(id); else n.add(id); return n; });
   149→   149→  const toggleTopic = (id: string) => setExpandedTopics((prev) => { const n = new Set(prev); if (n.has(id)) n.delete(id); else n.add(id); return n; });
   150→   150→
   151→   151→  // ─── Fit zoom ──────────────────────────────────────────────
   152→   152→  const computeFitScale = React.useCallback(() => {
   153→   153→    const el = containerRef.current;
   154→   154→    if (!el) return;
   155→   155→    const w = el.clientWidth - 48;
   156→   156→    setZoom(Math.max(0.25, Math.min(w / 1136, 1)));
   157→   157→  }, []);
   158→   158→
   159→   159→  React.useEffect(() => {
   160→   160→    if (zoomMode === "fit") { const t = setTimeout(computeFitScale, 80); return () => clearTimeout(t); }
   161→   161→  }, [note, zoomMode, computeFitScale, sidebarCollapsed]);
   162→   162→
   163→   163→  React.useEffect(() => {
   164→   164→    if (zoomMode !== "fit") return;
   165→   165→    const h = () => computeFitScale();
   166→   166→    window.addEventListener("resize", h);
   167→   167→    return () => window.removeEventListener("resize", h);
   168→   168→  }, [zoomMode, computeFitScale]);
   169→   169→
   170→   170→  // ─── Fullscreen ────────────────────────────────────────────
   171→   171→  const toggleFullscreen = React.useCallback(() => {
   172→   172→    if (document.fullscreenElement) document.exitFullscreen?.();
   173→   173→    else containerRef.current?.requestFullscreen?.();
   174→   174→  }, []);
   175→   175→  React.useEffect(() => { const h = () => setIsFullscreen(!!document.fullscreenElement); document.addEventListener("fullscreenchange", h); return () => document.removeEventListener("fullscreenchange", h); }, []);
   176→   176→
   177→   177→  // ─── Close popovers ────────────────────────────────────────
   178→   178→  React.useEffect(() => {
   179→   179→    const handler = (e: MouseEvent) => {
   180→   180→      if (downloadOpen && downloadRef.current && !downloadRef.current.contains(e.target as Node)) setDownloadOpen(false);
   181→   181→      if (zoomPresetsOpen && zoomRef.current && !zoomRef.current.contains(e.target as Node)) setZoomPresetsOpen(false);
   182→   182→    };
   183→   183→    document.addEventListener("mousedown", handler);
   184→   184→    return () => document.removeEventListener("mousedown", handler);
   185→   185→  }, [downloadOpen, zoomPresetsOpen]);
   186→   186→
   187→   187→  // ─── Keyboard ──────────────────────────────────────────────
   188→   188→  React.useEffect(() => {
   189→   189→    const handleKey = (e: KeyboardEvent) => {
   190→   190→      const t = e.target as HTMLElement;
   191→   191→      const typing = t.tagName === "INPUT" || t.tagName === "TEXTAREA";
   192→   192→      if (e.key === "Escape") {
   193→   193→        if (mobileSidebarOpen) { setMobileSidebarOpen(false); return; }
   194→   194→        if (downloadOpen) { setDownloadOpen(false); return; }
   195→   195→        if (zoomPresetsOpen) { setZoomPresetsOpen(false); return; }
   196→   196→        setToolbarExpanded(false);
   197→   197→      }
   198→   198→      if ((e.ctrlKey || e.metaKey) && (e.key === "+" || e.key === "=")) { e.preventDefault(); setZoomMode("manual"); setZoom(z => Math.min(4, z + 0.1)); return; }
   199→   199→      if ((e.ctrlKey || e.metaKey) && e.key === "-") { e.preventDefault(); setZoomMode("manual"); setZoom(z => Math.max(0.25, z - 0.1)); return; }
   200→   200→      if ((e.ctrlKey || e.metaKey) && e.key === "0") { e.preventDefault(); setZoomMode("fit"); computeFitScale(); return; }
   201→   201→      if (typing) return;
   202→   202→      if (e.key === "ArrowLeft" && !mobileSidebarOpen && prevPage) { e.preventDefault(); handleNavigate(prevPage); }
   203→   203→      if (e.key === "ArrowRight" && !mobileSidebarOpen && nextPage) { e.preventDefault(); handleNavigate(nextPage); }
   204→   204→    };
   205→   205→    window.addEventListener("keydown", handleKey);
   206→   206→    return () => window.removeEventListener("keydown", handleKey);
   207→   207→  }, [mobileSidebarOpen, downloadOpen, zoomPresetsOpen, prevPage, nextPage, handleNavigate, computeFitScale]);
   208→   208→
   209→   209→  // ─── Iframe ────────────────────────────────────────────────
   210→   210→  const handleIframeLoad = React.useCallback((e: React.SyntheticEvent<HTMLIFrameElement>) => {
   211→   211→    try {
   212→   212→      const iframe = e.currentTarget;
   213→   213→      const doc = iframe.contentDocument;
   214→   214→      if (!doc) return;
   215→   215→      const ex = doc.getElementById("reader-style");
   216→   216→      if (ex) ex.remove();
   217→   217→      const style = doc.createElement("style");
   218→   218→      style.id = "reader-style";
   219→   219→      style.textContent = `body { justify-content: flex-start !important; padding-left: 42px !important; padding-right: 14px !important; } @media (max-width: 1120px) { html { overflow-x: auto; } }`;
   220→   220→      doc.head.appendChild(style);
   221→   221→      const resize = () => { const h = Math.max(doc.body.scrollHeight, doc.body.offsetHeight, doc.documentElement.scrollHeight, doc.documentElement.offsetHeight); iframe.style.height = `${h + 20}px`; };
   222→   222→      resize(); setTimeout(resize, 500); setTimeout(resize, 1500);
   223→   223→    } catch {}
   224→   224→  }, []);
   225→   225→
   226→   226→  const handleShare = () => { navigator.clipboard?.writeText(window.location.href).then(() => { setCopied(true); setTimeout(() => setCopied(false), 2000); }); };
   227→   227→
   228→   228→  // ─── Render ────────────────────────────────────────────────
   229→   229→  if (loading) return (<div className="flex h-screen items-center justify-center bg-slate-900"><Skeleton className="h-[60vh] w-full max-w-4xl rounded-xl" /></div>);
   230→   230→  if (!note) return (<div className="flex h-screen flex-col items-center justify-center gap-4 bg-slate-900 text-slate-200"><Home className="size-8 text-slate-400" /><h2 className="text-xl font-bold text-white">Note not found</h2><Button onClick={goHome} className="gap-2"><Home className="size-4" /> Browse all notes</Button></div>);
   231→   231→
   232→   232→  const zoomLabel = zoomMode === "fit" ? "Fit" : `${Math.round(zoom * 100)}%`;
   233→   233→
   234→   234→  // Filter tree
   235→   235→  const filteredTree = sidebarSearch.trim()
   236→   236→    ? tree.map((node) => {
   237→   237→        const q = sidebarSearch.toLowerCase();
   238→   238→        return {
   239→   239→          ...node,
   240→   240→          topics: node.topics.map(t => ({ ...t, notes: t.notes.filter(n => n.title.toLowerCase().includes(q)) })).filter(t => t.notes.length > 0),
   241→   241→          notes: node.notes.filter(n => n.title.toLowerCase().includes(q)),
   242→   242→        };
   243→   243→      }).filter(n => n.topics.length > 0 || n.notes.length > 0)
   244→   244→    : tree;
   245→   245→
   246→   246→  return (
   247→   247→    <div ref={containerRef} className="flex h-screen w-full overflow-hidden bg-slate-900 text-slate-100">
   248→   248→      {/* ═══ Mobile backdrop ═══ */}
   249→   249→      <AnimatePresence>
   250→   250→        {mobileSidebarOpen && (
   251→   251→          <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
   252→   252→            className="fixed inset-0 z-40 bg-black/50 md:hidden" onClick={() => setMobileSidebarOpen(false)} />
   253→   253→        )}
   254→   254→      </AnimatePresence>
   255→   255→
   256→   256→      {/* ═══ Left Sidebar (desktop inline) ═══ */}
   257→   257→      <motion.aside
   258→   258→        animate={{ width: sidebarCollapsed ? 0 : 280 }}
   259→   259→        transition={{ type: "spring", damping: 30, stiffness: 300 }}
   260→   260→        className="relative z-30 hidden shrink-0 overflow-hidden border-r border-slate-800 bg-slate-950 md:block"
   261→   261→      >
   262→   262→        <SidebarContent
   263→   263→          tree={filteredTree} note={note} expandedCats={expandedCats} expandedTopics={expandedTopics}
   264→   264→          toggleCat={toggleCat} toggleTopic={toggleTopic} onNavigate={handleNavigate}
   265→   265→          sidebarMode={sidebarMode} setSidebarMode={setSidebarMode}
   266→   266→          sidebarSearch={sidebarSearch} setSidebarSearch={setSidebarSearch} goHome={goHome}
   267→   267→        />
   268→   268→      </motion.aside>
   269→   269→
   270→   270→      {/* ═══ Left Sidebar (mobile drawer) ═══ */}
   271→   271→      <AnimatePresence>
   272→   272→        {mobileSidebarOpen && (
   273→   273→          <motion.aside
   274→   274→            initial={{ x: "-100%" }} animate={{ x: 0 }} exit={{ x: "-100%" }}
   275→   275→            transition={{ type: "spring", damping: 30, stiffness: 300 }}
   276→   276→            className="fixed inset-y-0 left-0 z-50 w-[85vw] max-w-[320px] overflow-hidden border-r border-slate-800 bg-slate-950 md:hidden"
   277→   277→          >
   278→   278→            <SidebarContent
   279→   279→              tree={filteredTree} note={note} expandedCats={expandedCats} expandedTopics={expandedTopics}
   280→   280→              toggleCat={toggleCat} toggleTopic={toggleTopic} onNavigate={handleNavigate}
   281→   281→              sidebarMode={sidebarMode} setSidebarMode={setSidebarMode}
   282→   282→              sidebarSearch={sidebarSearch} setSidebarSearch={setSidebarSearch} goHome={goHome}
   283→   283→            />
   284→   284→          </motion.aside>
   285→   285→        )}
   286→   286→      </AnimatePresence>
   287→   287→
   288→   288→      {/* ═══ Main Area ═══ */}
   289→   289→      <div className="flex min-w-0 flex-1 flex-col">
   290→   290→        {/* ── Top Bar ── */}
   291→   291→        <div className="flex h-12 shrink-0 items-center gap-2 border-b border-slate-800 bg-slate-950/80 px-3 backdrop-blur-md">
   292→   292→          <Button variant="ghost" size="icon" className="size-8 shrink-0 text-slate-400 hover:text-slate-200"
   293→   293→            onClick={() => { if (window.innerWidth >= 768) setSidebarCollapsed(!sidebarCollapsed); else setMobileSidebarOpen(true); }}
   294→   294→            title="Toggle sidebar">
   295→   295→            {sidebarCollapsed ? <PanelLeftOpen className="size-4" /> : <PanelLeftClose className="size-4" />}
   296→   296→          </Button>
   297→   297→          <Button variant="ghost" size="icon" className="size-8 shrink-0 text-slate-400 hover:text-slate-200" onClick={goHome} title="Home">
   298→   298→            <Home className="size-4" />
   299→   299→          </Button>
   300→   300→          <div className="hidden min-w-0 flex-1 items-center gap-1.5 text-xs text-slate-400 sm:flex">
   301→   301→            {note.category && <span className="truncate">{note.category.name}</span>}
   302→   302→            <ChevronRight className="size-3 shrink-0 text-slate-600" />
   303→   303→            <span className="truncate text-slate-200">{note.title}</span>
   304→   304→            {totalPages > 1 && <span className="ml-1 text-slate-500">· {currentIndex + 1}/{totalPages}</span>}
   305→   305→          </div>
   306→   306→          <div className="flex-1 sm:hidden" />
   307→   307→          <Button variant="ghost" size="icon" className="size-8 shrink-0 text-slate-400 hover:text-slate-200"
   308→   308→            onClick={() => setRightPanelOpen(!rightPanelOpen)} title="Toggle info">
   309→   309→            <MoreVertical className="size-4" />
   310→   310→          </Button>
   311→   311→          <Button variant="ghost" size="icon" className="size-8 shrink-0 text-slate-400 hover:text-slate-200"
   312→   312→            onClick={toggleFullscreen} title="Fullscreen">
   313→   313→            {isFullscreen ? <Minimize className="size-4" /> : <Maximize className="size-4" />}
   314→   314→          </Button>
   315→   315→        </div>
   316→   316→
   317→   317→        {/* ── Floating Toolbar ── */}
   318→   318→        <AnimatePresence>
   319→   319→          {toolbarExpanded && (
   320→   320→            <motion.div
   321→   321→              initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }}
   322→   322→              transition={{ duration: 0.2 }}
   323→   323→              className="pointer-events-none absolute left-1/2 top-14 z-20 -translate-x-1/2"
   324→   324→            >
   325→   325→              <div className="pointer-events-auto flex items-center gap-1 rounded-full border border-slate-700 bg-slate-800/95 px-2 py-1.5 shadow-lg backdrop-blur-md">
   326→   326→                <div className="relative" ref={downloadRef}>
   327→   327→                  <Button variant="ghost" size="icon" className={cn("size-7 rounded-full text-slate-400 hover:text-slate-200", downloadOpen && "bg-slate-700")}
   328→   328→                    onClick={() => setDownloadOpen(!downloadOpen)} title="Export">
   329→   329→                    <Download className="size-3.5" />
   330→   330→                  </Button>
   331→   331→                  <AnimatePresence>
   332→   332→                    {downloadOpen && (
   333→   333→                      <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}
   334→   334→                        className="absolute left-0 top-full mt-2 w-48 rounded-lg border border-slate-700 bg-slate-800 p-1 shadow-xl">
   335→   335→                        <div className="px-2 py-1 text-[10px] font-bold uppercase tracking-wider text-slate-500">Current Page</div>
   336→   336→                        {["PNG", "JPEG", "PDF"].map(f => <button key={f} onClick={() => setDownloadOpen(false)} className="flex w-full items-center gap-2 rounded px-2 py-1.5 text-xs text-slate-300 hover:bg-slate-700"><FileImage className="size-3.5 text-slate-500" /> {f}</button>)}
   337→   337→                      </motion.div>
   338→   338→                    )}
   339→   339→                  </AnimatePresence>
   340→   340→                </div>
   341→   341→                <Sep />
   342→   342→                <Button variant="ghost" size="icon" disabled={!prevPage} className="size-7 rounded-full text-slate-400 hover:text-slate-200" onClick={() => prevPage && handleNavigate(prevPage)} title="Prev (←)"><ChevronLeft className="size-3.5" /></Button>
   343→   343→                <span className="min-w-[3rem] text-center text-xs font-mono text-slate-400">{currentIndex + 1}/{totalPages}</span>
   344→   344→                <Button variant="ghost" size="icon" disabled={!nextPage} className="size-7 rounded-full text-slate-400 hover:text-slate-200" onClick={() => nextPage && handleNavigate(nextPage)} title="Next (→)"><ChevronRight className="size-3.5" /></Button>
   345→   345→                <Sep />
   346→   346→                <Button variant="ghost" size="icon" className="size-7 rounded-full text-slate-400 hover:text-slate-200" onClick={() => { setZoomMode("manual"); setZoom(Math.max(0.25, zoom - 0.1)); }} title="Zoom out"><ZoomOut className="size-3.5" /></Button>
   347→   347→                <div className="relative" ref={zoomRef}>
   348→   348→                  <button className="h-6 min-w-[44px] rounded border border-transparent bg-slate-700 px-1 text-center text-xs font-mono text-slate-300 hover:border-slate-600" onClick={() => setZoomPresetsOpen(!zoomPresetsOpen)}>{zoomLabel}</button>
   349→   349→                  <AnimatePresence>
   350→   350→                    {zoomPresetsOpen && (
   351→   351→                      <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}
   352→   352→                        className="absolute left-1/2 top-full mt-2 w-32 -translate-x-1/2 rounded-lg border border-slate-700 bg-slate-800 p-1 shadow-xl">
   353→   353→                        <button onClick={() => { setZoomMode("fit"); computeFitScale(); setZoomPresetsOpen(false); }} className="flex w-full items-center gap-2 rounded px-2 py-1 text-xs text-slate-300 hover:bg-slate-700"><Maximize className="size-3" /> Fit</button>
   354→   354→                        <div className="my-1 h-px bg-slate-700" />
   355→   355→                        {["50%", "75%", "100%", "125%", "150%"].map(l => <button key={l} onClick={() => { setZoomMode("manual"); setZoom(parseInt(l)/100); setZoomPresetsOpen(false); }} className="flex w-full rounded px-2 py-1 text-xs text-slate-300 hover:bg-slate-700">{l}</button>)}
   356→   356→                      </motion.div>
   357→   357→                    )}
   358→   358→                  </AnimatePresence>
   359→   359→                </div>
   360→   360→                <Button variant="ghost" size="icon" className="size-7 rounded-full text-slate-400 hover:text-slate-200" onClick={() => { setZoomMode("manual"); setZoom(Math.min(4, zoom + 0.1)); }} title="Zoom in"><ZoomIn className="size-3.5" /></Button>
   361→   361→                <Sep />
   362→   362→                <Button variant="ghost" size="icon" className="size-7 rounded-full text-slate-400 hover:text-slate-200" onClick={() => setToolbarExpanded(false)} title="Collapse"><ChevronUp className="size-3.5" /></Button>
   363→   363→              </div>
   364→   364→            </motion.div>
   365→   365→          )}
   366→   366→        </AnimatePresence>
   367→   367→
   368→   368→        {!toolbarExpanded && (
   369→   369→          <button onClick={() => setToolbarExpanded(true)} className="absolute left-1/2 top-14 z-20 flex -translate-x-1/2 items-center gap-1 rounded-full border border-slate-700 bg-slate-800/95 px-3 py-1 text-xs text-slate-400 shadow-md backdrop-blur-sm hover:text-slate-200" title="Expand toolbar">
   370→   370→            <ChevronDown className="size-3" /><span className="font-mono">{currentIndex + 1}/{totalPages}</span>
   371→   371→          </button>
   372→   372→        )}
   373→   373→
   374→   374→        {/* ── Document ── */}
   375→   375→        <div className="flex-1 overflow-auto bg-slate-900 p-4 pt-12">
   376→   376→          <AnimatePresence mode="wait">
   377→   377→            <motion.div key={note.id} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} transition={{ duration: 0.25 }}
   378→   378→              className="mx-auto overflow-x-auto rounded-xl bg-white shadow-2xl" style={{ maxWidth: `${1080 + 56}px`, width: "100%" }}>
   379→   379→              <iframe title={note.title} className="block" style={{ minHeight: "60vh", transform: `scale(${zoom})`, transformOrigin: "top left", width: `${100 / zoom}%`, border: 0 }}
   380→   380→                src={note.contentPath} sandbox="allow-same-origin allow-popups" onLoad={handleIframeLoad} />
   381→   381→            </motion.div>
   382→   382→          </AnimatePresence>
   383→   383→
   384→   384→          {totalPages > 1 && (
   385→   385→            <div className="mx-auto mt-4 flex max-w-3xl items-center justify-between border-t border-slate-800 pt-3">
   386→   386→              {prevPage ? <Button variant="ghost" size="sm" onClick={() => handleNavigate(prevPage)} className="gap-1.5 text-slate-400 hover:text-slate-200"><ChevronLeft className="size-4" /><span className="hidden max-w-[200px] truncate sm:inline">{prevPage.title}</span><span className="sm:hidden">Prev</span></Button> : <div />}
   387→   387→              {nextPage ? <Button variant="ghost" size="sm" onClick={() => handleNavigate(nextPage)} className="gap-1.5 text-slate-400 hover:text-slate-200"><span className="hidden max-w-[200px] truncate sm:inline">{nextPage.title}</span><span className="sm:hidden">Next</span><ChevronRight className="size-4" /></Button> : <div />}
   388→   388→            </div>
   389→   389→          )}
   390→   390→        </div>
   391→   391→      </div>
   392→   392→
   393→   393→      {/* ═══ Right Panel ═══ */}
   394→   394→      <AnimatePresence>
   395→   395→        {rightPanelOpen && (
   396→   396→          <motion.aside initial={{ width: 0, opacity: 0 }} animate={{ width: 240, opacity: 1 }} exit={{ width: 0, opacity: 0 }} transition={{ type: "spring", damping: 30, stiffness: 300 }}
   397→   397→            className="hidden shrink-0 overflow-hidden border-l border-slate-800 bg-slate-950 lg:block">
   398→   398→            <RightPanel note={note} pages={pages} currentIndex={currentIndex} relatedNotes={relatedNotes} onNavigate={handleNavigate} copied={copied} onShare={handleShare} />
   399→   399→          </motion.aside>
   400→   400→        )}
   401→   401→      </AnimatePresence>
   402→   402→    </div>
   403→   403→  );
   404→   404→}
   405→   405→
   406→   406→// ─── Sidebar Content ───────────────────────────────────────────
   407→   407→function SidebarContent({ tree, note, expandedCats, expandedTopics, toggleCat, toggleTopic, onNavigate, sidebarMode, setSidebarMode, sidebarSearch, setSidebarSearch, goHome }: {
   408→   408→  tree: TreeNode[]; note: Note; expandedCats: Set<string>; expandedTopics: Set<string>;
   409→   409→  toggleCat: (id: string) => void; toggleTopic: (id: string) => void; onNavigate: (n: Note) => void;
   410→   410→  sidebarMode: SidebarMode; setSidebarMode: (m: SidebarMode) => void;
   411→   411→  sidebarSearch: string; setSidebarSearch: (s: string) => void; goHome: () => void;
   412→   412→}) {
   413→   413→  return (
   414→   414→    <div className="flex h-full w-[280px] flex-col">
   415→   415→      {/* Header */}
   416→   416→      <div className="flex items-center justify-between border-b border-slate-800 px-3 py-2.5">
   417→   417→        <button onClick={goHome} className="flex items-center gap-2 text-sm font-semibold text-slate-200 hover:text-white">
   418→   418→          <Home className="size-4 text-slate-400" /> Notes
   419→   419→        </button>
   420→   420→        <button className="text-slate-500 hover:text-slate-300 md:hidden" onClick={() => {}}>
   421→   421→          <X className="size-4" />
   422→   422→        </button>
   423→   423→      </div>
   424→   424→
   425→   425→      {/* Search */}
   426→   426→      <div className="border-b border-slate-800 px-3 py-2">
   427→   427→        <div className="relative">
   428→   428→          <Search className="pointer-events-none absolute left-2.5 top-1/2 size-3.5 -translate-y-1/2 text-slate-600" />
   429→   429→          <input value={sidebarSearch} onChange={(e) => setSidebarSearch(e.target.value)} placeholder="Filter topics…"
   430→   430→            className="w-full rounded-md border border-slate-800 bg-slate-900 py-1.5 pl-8 pr-2 text-xs text-slate-300 outline-none placeholder:text-slate-600 focus:border-slate-700" />
   431→   431→        </div>
   432→   432→      </div>
   433→   433→
   434→   434→      {/* View toggle */}
   435→   435→      <div className="flex items-center gap-1 border-b border-slate-800 px-3 py-1.5">
   436→   436→        <div className="flex items-center gap-0.5 rounded-md bg-slate-800 p-0.5">
   437→   437→          <button onClick={() => setSidebarMode("list")} className={cn("flex items-center gap-1 rounded px-2 py-0.5 text-xs transition-all", sidebarMode === "list" ? "bg-slate-700 font-semibold text-slate-100" : "text-slate-500")}>
   438→   438→            <ChevronRight className="size-3" /> List
   439→   439→          </button>
   440→   440→          <button onClick={() => setSidebarMode("tile")} className={cn("flex items-center gap-1 rounded px-2 py-0.5 text-xs transition-all", sidebarMode === "tile" ? "bg-slate-700 font-semibold text-slate-100" : "text-slate-500")}>
   441→   441→            <FileText className="size-3" /> Tiles
   442→   442→          </button>
   443→   443→        </div>
   444→   444→      </div>
   445→   445→
   446→   446→      {/* Tree */}
   447→   447→      <ScrollArea className="flex-1 px-1 py-1">
   448→   448→        {tree.length === 0 ? (
   449→   449→          <div className="px-3 py-8 text-center text-xs text-slate-600">No notes found</div>
   450→   450→        ) : (
   451→   451→          <div className="space-y-0.5 pb-4">
   452→   452→            {tree.map((node) => {
   453→   453→              const isExpanded = expandedCats.has(node.category.id) || !!sidebarSearch.trim();
   454→   454→              const allNotes = [...node.notes, ...node.topics.flatMap(t => t.notes)];
   455→   455→              const isActiveCat = allNotes.some(n => n.id === note.id);
   456→   456→              return (
   457→   457→                <div key={node.category.id}>
   458→   458→                  {/* Category */}
   459→   459→                  <button onClick={() => toggleCat(node.category.id)}
   460→   460→                    className={cn("group flex w-full items-center gap-1.5 rounded-md px-2 py-1.5 text-left text-xs font-medium transition-colors",
   461→   461→                      isActiveCat ? "bg-slate-800 text-slate-200" : "text-slate-400 hover:bg-slate-900 hover:text-slate-300")}>
   462→   462→                    <ChevronDown className={cn("size-3 shrink-0 transition-transform text-slate-600", !isExpanded && "-rotate-90")} />
   463→   463→                    {isExpanded ? <FolderOpen className="size-3.5 shrink-0 text-slate-500" /> : <Folder className="size-3.5 shrink-0 text-slate-500" />}
   464→   464→                    <span className="truncate">{node.category.name}</span>
   465→   465→                    <span className="ml-auto text-[10px] text-slate-600">{allNotes.length}</span>
   466→   466→                  </button>
   467→   467→
   468→   468→                  {/* Expanded content */}
   469→   469→                  <AnimatePresence>
   470→   470→                    {isExpanded && (
   471→   471→                      <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.2 }} className="overflow-hidden">
   472→   472→                        <div className="ml-3 border-l border-slate-800 pl-2">
   473→   473→                          {/* Direct notes */}
   474→   474→                          {node.notes.map((n, idx) => (
   475→   475→                            <PageItem key={n.id} note={n} index={idx} isActive={n.id === note.id} onNavigate={onNavigate} mode={sidebarMode} />
   476→   476→                          ))}
   477→   477→                          {/* Topics */}
   478→   478→                          {node.topics.map((topic) => {
   479→   479→                            const tExp = expandedTopics.has(topic.category.id) || !!sidebarSearch.trim();
   480→   480→                            const isActiveT = topic.notes.some(n => n.id === note.id);
   481→   481→                            return (
   482→   482→                              <div key={topic.category.id}>
   483→   483→                                <button onClick={() => toggleTopic(topic.category.id)}
   484→   484→                                  className={cn("flex w-full items-center gap-1.5 rounded-md px-2 py-1 text-left text-xs transition-colors",
   485→   485→                                    isActiveT ? "text-slate-200" : "text-slate-500 hover:text-slate-300")}>
   486→   486→                                  <ChevronDown className={cn("size-3 shrink-0 transition-transform text-slate-600", !tExp && "-rotate-90")} />
   487→   487→                                  <span className="truncate">{topic.category.name}</span>
   488→   488→                                  <span className="ml-auto text-[10px] text-slate-600">{topic.notes.length}</span>
   489→   489→                                </button>
   490→   490→                                <AnimatePresence>
   491→   491→                                  {tExp && (
   492→   492→                                    <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.2 }} className="overflow-hidden">
   493→   493→                                      <div className="ml-3 border-l border-slate-800 pl-2">
   494→   494→                                        {topic.notes.map((n, idx) => (
   495→   495→                                          <PageItem key={n.id} note={n} index={idx} isActive={n.id === note.id} onNavigate={onNavigate} mode={sidebarMode} />
   496→   496→                                        ))}
   497→   497→                                      </div>
   498→   498→                                    </motion.div>
   499→   499→                                  )}
   500→   500→                                </AnimatePresence>
   501→   501→                              </div>
   502→   502→                            );
   503→   503→                          })}
   504→   504→                        </div>
   505→   505→                      </motion.div>
   506→   506→                    )}
   507→   507→                  </AnimatePresence>
   508→   508→                </div>
   509→   509→              );
   510→   510→            })}
   511→   511→          </div>
   512→   512→        )}
   513→   513→      </ScrollArea>
   514→   514→    </div>
   515→   515→  );
   516→   516→}
   517→   517→
   518→   518→// ─── Page Item ────────────────────────────────────────────────
   519→   519→function PageItem({ note: n, index, isActive, onNavigate, mode }: { note: Note; index: number; isActive: boolean; onNavigate: (n: Note) => void; mode: SidebarMode }) {
   520→   520→  if (mode === "tile") {
   521→   521→    return (
   522→   522→      <button onClick={() => onNavigate(n)} className={cn("mb-1 block w-full overflow-hidden rounded-lg border text-left transition-colors", isActive ? "border-rose-500/50 bg-rose-500/10" : "border-slate-800 hover:border-slate-700")}>
   523→   523→        <div className="flex items-center gap-1.5 px-2 py-1">
   524→   524→          <span className={cn("text-[10px] font-mono", isActive ? "text-rose-400" : "text-slate-600")}>{String(index + 1).padStart(2, "0")}</span>
   525→   525→          <span className={cn("truncate text-[11px]", isActive ? "font-medium text-rose-300" : "text-slate-400")}>{n.title}</span>
   526→   526→        </div>
   527→   527→      </button>
   528→   528→    );
   529→   529→  }
   530→   530→  return (
   531→   531→    <button onClick={() => onNavigate(n)} className={cn("flex w-full items-center gap-2 rounded-md px-2 py-1.5 text-left text-xs transition-colors", isActive ? "bg-rose-500/10 text-rose-300" : "text-slate-400 hover:bg-slate-900 hover:text-slate-300")}>
   532→   532→      <span className={cn("flex size-4 shrink-0 items-center justify-center rounded text-[9px] font-mono", isActive ? "bg-rose-500 text-white" : "bg-slate-800 text-slate-600")}>{index + 1}</span>
   533→   533→      <span className={cn("truncate", isActive && "font-medium")}>{n.title}</span>
   534→   534→      {isActive && <span className="ml-auto size-1.5 shrink-0 rounded-full bg-rose-500" />}
   535→   535→    </button>
   536→   536→  );
   537→   537→}
   538→   538→
   539→   539→// ─── Right Panel ───────────────────────────────────────────────
   540→   540→function RightPanel({ note, pages, currentIndex, relatedNotes, onNavigate, copied, onShare }: {
   541→   541→  note: Note; pages: Note[]; currentIndex: number; relatedNotes: Note[]; onNavigate: (n: Note) => void; copied: boolean; onShare: () => void;
   542→   542→}) {
   543→   543→  return (
   544→   544→    <div className="flex h-full w-[240px] flex-col overflow-y-auto">
   545→   545→      <div className="border-b border-slate-800 p-3">
   546→   546→        <h3 className="mb-2 text-[10px] font-bold uppercase tracking-wider text-slate-600">Info</h3>
   547→   547→        <div className="space-y-1 text-xs text-slate-500">
   548→   548→          <div className="flex items-center gap-1.5"><Clock className="size-3" /> {fromNow(note.updatedAt)}</div>
   549→   549→          <div>{formatDateTime(note.updatedAt)}</div>
   550→   550→        </div>
   551→   551→        {note.tags && note.tags.length > 0 && (
   552→   552→          <div className="mt-2 flex flex-wrap gap-1">{note.tags.map(t => <Badge key={t.id} variant="outline" className="border-slate-700 bg-slate-800 text-[10px] font-normal text-slate-400">{t.name}</Badge>)}</div>
   553→   553→        )}
   554→   554→      </div>
   555→   555→      <div className="border-b border-slate-800 p-3">
   556→   556→        <h3 className="mb-2 text-[10px] font-bold uppercase tracking-wider text-slate-600">Actions</h3>
   557→   557→        <div className="space-y-0.5">
   558→   558→          <button onClick={onShare} className="flex w-full items-center gap-2 rounded px-2 py-1.5 text-xs text-slate-400 hover:bg-slate-900 hover:text-slate-300"><Share2 className="size-3.5" /> {copied ? "Link Copied!" : "Copy Link"}</button>
   559→   559→          <a href={note.contentPath} target="_blank" rel="noopener noreferrer" className="flex w-full items-center gap-2 rounded px-2 py-1.5 text-xs text-slate-400 hover:bg-slate-900 hover:text-slate-300"><ExternalLink className="size-3.5" /> Open Raw</a>
   560→   560→        </div>
   561→   561→      </div>
   562→   562→      {relatedNotes.length > 0 && (
   563→   563→        <div className="border-b border-slate-800 p-3">
   564→   564→          <h3 className="mb-2 text-[10px] font-bold uppercase tracking-wider text-slate-600">Related</h3>
   565→   565→          <div className="space-y-0.5">{relatedNotes.map(rel => <button key={rel.id} onClick={() => onNavigate(rel)} className="flex w-full items-center gap-2 rounded px-2 py-1.5 text-left text-xs text-slate-400 hover:bg-slate-900 hover:text-slate-300"><FileText className="size-3 shrink-0 text-slate-600" /><span className="truncate">{rel.title}</span></button>)}</div>
   566→   566→        </div>
   567→   567→      )}
   568→   568→      {pages.length > 1 && (
   569→   569→        <div className="p-3">
   570→   570→          <h3 className="mb-2 text-[10px] font-bold uppercase tracking-wider text-slate-600">All Pages</h3>
   571→   571→          <div className="space-y-0.5">{pages.map((p, idx) => <button key={p.id} onClick={() => onNavigate(p)} className={cn("flex w-full items-center gap-2 rounded px-2 py-1 text-left text-xs transition-colors", p.id === note.id ? "font-semibold text-rose-400" : "text-slate-500 hover:text-slate-300")}><span className="w-4 shrink-0 font-mono text-[10px]">{String(idx + 1).padStart(2, "0")}.</span><span className="truncate">{p.title}</span></button>)}</div>
   572→   572→        </div>
   573→   573→      )}
   574→   574→    </div>
   575→   575→  );
   576→   576→}
   577→   577→
   578→   578→function Sep() { return <div className="mx-0.5 h-4 w-px bg-slate-700" />; }
   579→   579→